---
  # FPM Pool user and group
  apache_user: apache
  apache_group: apache

  # php.ini config variables
  short_open_tag: On
  memory_limit: 2G
  date_timezone: UTC
  max_execution_time: 300
  max_input_time: 120
  post_max_size: 1024M
  upload_max_filesize: 1024M
